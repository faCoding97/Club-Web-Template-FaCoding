# ElixFit Club — Next.js (App Router) + Tailwind + TypeScript
All content from `/data/site.json`. Sporty look with pill buttons and gradient hero. Contact form is a Client Component.
## Quickstart
npm install
npm run dev
