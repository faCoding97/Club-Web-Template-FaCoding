import Container from '@/components/Container'; import Section from '@/components/Section'; import ClassCard from '@/components/ClassCard'; import siteData from '@/data/site.json'; import type { Metadata } from 'next'; export const metadata: Metadata={title:`Classes • ${siteData.site.name}`,description:'Browse all classes'}; export default function ClassesPage(){ return(<Container><Section title='All Classes' description='Choose your track and start training.'><div className='grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6'>{siteData.classes.map(item=>(<ClassCard key={item.slug} item={item}/>))}</div></Section></Container>); }