import Image from 'next/image'; import Link from 'next/link'; import Container from '@/components/Container'; import Section from '@/components/Section'; import ClassCard from '@/components/ClassCard'; import QRCodeCard from '@/components/QRCodeCard'; import siteData from '@/data/site.json'; export default function HomePage(){ const hero=siteData.hero; const featured=siteData.classes.slice(0,3); return(<><section className='relative overflow-hidden'><div className='absolute inset-0 bg-gradient-to-r from-fit-100 via-white to-fit-100'/><Container><div className='relative grid grid-cols-1 md:grid-cols-2 items-center gap-10 py-12 sm:py-16'><div><h1 className='text-4xl sm:text-5xl font-extrabold tracking-tight'>{hero.title}</h1><p className='mt-4 text-lg text-gray-700'>{hero.subtitle}</p><div className='mt-6'><Link href={hero.ctaHref} className='btn-pill'>{hero.ctaLabel}</Link></div></div>{hero.image?(<div className='relative w-full h-64 sm:h-80 md:h-96'><Image src={hero.image} alt='Fitness hero' fill sizes='(max-width: 768px) 100vw, 50vw' className='object-cover rounded-2xl shadow-soft'/></div>):null}</div></Container></section><Container><div className='mt-10'><QRCodeCard/></div><Section title='Featured Classes' description='Pick a class and meet your coach this week.'><div className='grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6'>{featured.map(item=>(<ClassCard key={item.slug} item={item}/>))}</div></Section></Container></>); }