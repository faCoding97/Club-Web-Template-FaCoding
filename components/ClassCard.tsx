import Image from 'next/image'; import Link from 'next/link'; import Tag from './Tag'; import type { GymClass } from '@/types/site'; export default function ClassCard({item}:{item:GymClass}){ return(<article className='card overflow-hidden h-full flex flex-col relative'><div className='absolute -left-8 -top-8 h-16 w-32 rotate-45 bg-fit-100 pointer-events-none'/><div className='relative h-44 w-full'><Image src={item.thumbnail} alt={`${item.title} cover`} fill sizes='(max-width: 768px) 100vw, 33vw' className='object-cover'/></div><div className='p-4 flex-1 flex flex-col gap-3'><div className='flex items-center justify-between'><h3 className='font-semibold'>{item.title}</h3><span className='text-xs rounded-pill bg-fit-100 border border-fit-200 px-3 py-1'>{item.level}</span></div><p className='text-sm text-gray-600'>{item.shortDescription}</p><div className='text-xs text-gray-700'>⏱ {item.duration} • 🗓 {item.schedule}</div><div className='flex flex-wrap gap-2 mt-auto'>{item.tags.map(t=>(<Tag key={t} label={t}/>))}</div></div><div className='p-4 pt-0'><Link href={`/classes/${item.slug}`} className='btn-pill w-full text-center'>View details</Link></div></article>); }