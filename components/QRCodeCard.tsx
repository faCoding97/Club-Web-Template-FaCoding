'use client'; import {useRef} from 'react'; import { QRCodeCanvas } from 'qrcode.react'; const qrUrl='https://elixfit.elixflare.com/classes'; export default function QRCodeCard(){ const ref=useRef<HTMLDivElement>(null); const handleDownload=()=>{ const canvas=ref.current?.querySelector('canvas'); if(!canvas) return; const dataUrl=canvas.toDataURL('image/png'); const a=document.createElement('a'); a.href=dataUrl; a.download='elixfit-classes-qr.png'; a.click(); }; return(<div className='card p-6'><h3 className='text-lg font-semibold'>Quick access</h3><p className='text-gray-600 mt-1'>Scan to jump to the Classes page.</p><div ref={ref} className='mt-4 flex items-center gap-6'><QRCodeCanvas value={qrUrl} size={144} includeMargin/><div className='space-y-2'><p className='text-sm break-all'>{qrUrl}</p><button onClick={handleDownload} className='btn-pill'>Download QR</button></div></div></div>); }